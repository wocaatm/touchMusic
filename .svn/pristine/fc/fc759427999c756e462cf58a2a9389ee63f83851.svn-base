import { SWITCH_MESSAGE_COUNT } from '../types'

const state = {
    count: 0
}

const mutations = {
    [SWITCH_MESSAGE_COUNT] (state, count) {
        state.count = count
    }
}

export default {
    state,
    mutations
}
